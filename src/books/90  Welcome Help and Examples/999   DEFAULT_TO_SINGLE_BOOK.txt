If a file exists in the 90... book folder with a title that contains the text

            DEFAULT_TO_SINGLE_BOOK 

then that will be the default setting shown to the user.  

Delete (or even better rename slightly) any such file to have the default be multi-book.

